import { createGlobalStyle } from 'styled-components';

const FontStyles = createGlobalStyle`

@font-face {
  font-family: 'Urbanist';
  src: url("https://fonts.googleapis.com/css2?family=Urbanist&display=swap"),
}`;

export default FontStyles;
